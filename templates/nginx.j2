events {
}
http {
    server{
        listen '{{ repo_http_port }}';
        root /usr/share/nginx/html;
        location / {
            autoindex on;
            }
        }
     }
pid /run/nginx.pid;
daemon off;